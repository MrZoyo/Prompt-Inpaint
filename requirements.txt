# Core dependencies
torch>=2.7
torchvision>=0.22

# SAM3 (Meta official package; requires gated access to facebook/sam3 on HF)
sam3 @ git+https://github.com/facebookresearch/sam3.git

# Required by sam3 / huggingface_hub for checkpoint download
huggingface_hub
einops

# Inpainting
iopaint>=1.2.0

# Utilities
numpy
pillow
opencv-python
pyyaml
requests
tqdm
setuptools
